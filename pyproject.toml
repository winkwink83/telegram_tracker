[project]
name = "telegram-tracker"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "faster-whisper",
    "requests",
    "python-dotenv",
]

[tool.setuptools.packages.find]
where = ["src"]