[project]
name = "telegram-tracker"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "faster-whisper",
    "requests",
    "python-dotenv",
    "google-generativeai>=0.8.6",
    "numpy>=2.2.6",
]

[tool.setuptools.packages.find]
where = ["src"]
